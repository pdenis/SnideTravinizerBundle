{% extends 'SnideTravinizerBundle::base.html.twig' %}

{% block main %}
    <div class="row">
        <div id="content">
            <div class="title">
                <h2>Update a repository</h2>
            </div>
            <form action="{{ path('snide_travinizer_repo_update', {'slug': slug}) }}" method="post" {{ form_enctype(form) }}>
                {% include 'SnideTravinizerBundle:Repo:form.html.twig' with {'form': form} %}
            </form>
        </div>
    </div>
{% endblock %}